<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.StockLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into stock_log
        (sku_id, business_type, biz_id, change_count, before_stock, after_stock, remark, create_time)
        values
        (#{skuId}, #{businessType}, #{bizId}, #{changeCount}, #{beforeStock}, #{afterStock}, #{remark}, #{createTime})
    </insert>
</mapper>
