<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.FarmerMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into farmer (name, phone, id_number, farm_name, origin_place, remark, status, create_time, update_time, create_user, update_user)
        values (#{name}, #{phone}, #{idNumber}, #{farmName}, #{originPlace}, #{remark}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>
    <update id="update" parameterType="com.sky.entity.Farmer">
        update farmer
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="idNumber != null">id_number = #{idNumber},</if>
            <if test="farmName != null">farm_name = #{farmName},</if>
            <if test="originPlace != null">origin_place = #{originPlace},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        </set>
        where id = #{id}
    </update>
    <select id="pageQuery" resultType="com.sky.entity.Farmer">
        select * from farmer
        <where>
            <if test="name != null and name != ''">and name like concat('%', #{name}, '%')</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by create_time desc
    </select>
    <select id="list" resultType="com.sky.entity.Farmer">
        select * from farmer
        <where>
            <if test="name != null and name != ''">and name like concat('%', #{name}, '%')</if>
            <if test="status != null">and status = #{status}</if>
        </where>
    </select>
</mapper>
